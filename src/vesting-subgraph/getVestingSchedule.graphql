query getVestingSchedule($id: ID!) {
  vestingSchedule(id: $id) {
    id
    cliffDate
    cliffAmount
    endDate
    flowRate
    receiver
    sender
    startDate
    superToken
    endExecutedAt
    deletedAt
    cliffAndFlowExecutedAt
  }
}